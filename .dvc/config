# MELprop-IADE DVC config.
# NOTE: `dvc init` could not be run in the cloud sandbox (dvc failed to install:
# antlr4-python3-runtime wheel build blocked). These files are hand-authored to
# match what `dvc init` + `dvc remote add -d` would produce, so no re-init is
# needed locally — just fill in the remote URL below and run `dvc pull`.
[core]
    remote = melpropnas
    autostage = true
['remote "melpropnas"']
    # TBD-HUMAN: replace with the real DVC-compatible endpoint for the team
    # storage informally called "melpropnas"/"melpropserver". EXACTLY what is
    # needed (none of this is documented anywhere in the repo — do not guess):
    #   - hostname / IP of the store
    #   - protocol + matching DVC remote type, ONE of:
    #       * an SMB share mounted locally      -> url = /mnt/melpropnas/iade-dvc  (local remote)
    #       * an S3-compatible endpoint (MinIO) -> url = s3://iade-dvc  + endpointurl below
    #   - credentials (for S3-compatible: access key id / secret, kept OUT of git)
    url = TBD-HUMAN
    # For an S3-compatible endpoint, also set (and keep secrets in a local, NON-committed
    # .dvc/config.local via `dvc remote modify --local`):
    #   endpointurl = https://<host>:<port>
