# DRAFT — not yet applied as .gitmodules. Content this repo's root
# .gitmodules would have once `git submodule add` is run for each entry
# below (pending separate explicit approval, per ADR-002).
#
# Do not copy this file to .gitmodules by hand — the correct way to apply
# it is via `git submodule add <url> <path>` per entry (which also records
# the pinned commit in the index), followed by `git checkout <ref>` inside
# each submodule to land on the exact pinned tag, then committing the
# result. Hand-copying this file without running `git submodule add` would
# leave .gitmodules present but the submodule gitlinks missing from the
# index — a broken half-state.

[submodule "external/suave"]
	path = external/suave
	url = https://github.com/suavecode/SUAVE
	# pin: tag 2.5.2 == 6554d2b3d1e7c2f1d4ba572aec99e1fd69d34a93

[submodule "external/pycycle"]
	path = external/pycycle
	url = https://github.com/OpenMDAO/pyCycle
	# pin: tag 4.1.2 == 5a6fe40059211312f4b6d86d1a2bb1d913073ce8
